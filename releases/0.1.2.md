A small follow-up to the first public beta build, published to take the
update path through its first real run: if Wymark offered you this version
and you are reading these notes inside the app, that path works. It carries no
other change.

## Highlights

- The first version delivered as an in-app update rather than a download.
- No other user-visible changes since 0.1.1.
